/** Both values currently reach the complete wire; only the short briefing still distinguishes them. */
export type ActionReach = 'both' | 'mcp'

export const ACTION_REACHES: readonly ActionReach[] = ['both', 'mcp']
