/**
 * How many pictures a shelf panel asks for.
 *
 * These are panels to glance at, not the asset browser: they page no further, and the number is
 * theirs rather than a user setting because a column has no bar to put one on. It matches what
 * these two read as bands — a column two tiles wide shows fewer at once, not more.
 */
export const PANEL_PAGE = 12
