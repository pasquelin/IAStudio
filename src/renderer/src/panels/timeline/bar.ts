/**
 * The box a timeline puts on the panel's title row.
 *
 * One declaration for the three of them — montage, animation, sound. Each had written its own,
 * and they drifted: the montage sat its buttons half a gutter apart, the animation three times
 * that, and the two bars read as controls of different sizes although both are `--sc-control`.
 */
export const TIMELINE_BAR = 'flex min-w-0 flex-1 items-center gap-0.5'
