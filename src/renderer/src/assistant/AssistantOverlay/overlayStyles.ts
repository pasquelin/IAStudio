/**
 * A block posted in the conversation column: the confirmation, and the place one writes. They sat
 * one above the other with the same eight classes and two different radii, which read as two
 * kinds of card where there is one.
 *
 * A sibling module rather than the overlay itself: the overlay imports the question, so the
 * question importing back would close a cycle `import-cycles.test.ts` keeps at zero.
 */
export const OVERLAY_CARD =
  'border-border bg-surface flex shrink-0 flex-col gap-2 rounded-(--radius-sc-lg) border p-2'
