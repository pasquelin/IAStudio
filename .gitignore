node_modules/
dist/
out/
coverage/
coverage-audit/
release/
.DS_Store
*.log
secrets/*
!secrets/.env.example
!secrets/README.md
.playwright-mcp/
# Doc externe régénérable (pnpm docs:scenario) — pas de la doc de projet
docs/scenario-api/
docs/specs
docs/superpowers
# Le dépôt ne publie que la DOC DU PROJET. Ce qui suit est du travail en cours ou de
# l'état d'outillage : utile sur la machine, hors sujet pour qui clone le dépôt.
docs/todo.md
docs/plans/
docs/design-system/
docs/prompt-loop.md
CLAUDE.md
.idea/
.vscode/
# Outillage des sessions Claude : il vit dans le dépôt, il n'en est pas le contenu.
# Les worktrees pèsent des gigaoctets et `skills/` n'est qu'un lien vers hors du dépôt.
.claude/
# Sorties d'exécution, jamais du code
logs/

# Shipped with the app, fetched by `pnpm ffmpeg:fetch` and `pnpm stt:fetch` — binaries and
# model weights, never committed.
resources/ffmpeg/
resources/stt/
src/renderer/public/decoders/

# Signing material. A safety net, never a permission: certificates belong in the GitHub
# secrets of the repository, and nowhere on disk — see docs/ci/SECRETS.md.
*.p12
*.pfx
*.cer
*.provisionprofile
*.mobileprovision
docs/todo-archive-*.md
